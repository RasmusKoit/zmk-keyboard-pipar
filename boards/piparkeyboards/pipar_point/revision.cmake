set(PIPAR_POINT_REVISIONS "left" "right")
if(NOT DEFINED BOARD_REVISION)
    set(BOARD_REVISION "right")
endif()
if(NOT BOARD_REVISION IN_LIST PIPAR_POINT_REVISIONS)
    message(FATAL_ERROR "Invalid BOARD_REVISION: ${BOARD_REVISION}. Valid revisions are: ${PIPAR_POINT_REVISIONS}")
endif()